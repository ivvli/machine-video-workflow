# 视频制作目录说明

这个目录用于产品短视频从素材、剪辑到成片交付的统一管理。

## 固定目录

- `原始素材/`：拍摄完成后先放这里，保持原始文件不修改。
- `视频工作区/`：剪辑过程中的项目文件、临时分析文件、音频、字幕等。
- `成片/`：最终导出的视频文件。
- `草稿/`：测试版、初剪版、需要确认的版本。
- `日志/`：项目复盘、规范、阶段记录。
- `工作流文档/`：拍摄、脚本、剪辑流程和模板。
- `工具环境/`：VoxCPM、CosyVoice、ConnectOnion等公共运行环境，不属于单个视频项目。
- `垃圾/`：暂时不用但不确定是否删除的旧视频或备份。

## 使用原则

- 原始素材尽量只复制不改动。
- 剪辑前先确认产品、受众、视频目的和前三秒钩子。
- 成片导出默认竖屏 9:16，优先 4K 清晰度。
- 视频分析和剪辑前审片产生的关键帧、镜头板、contact sheet、临时 clips 只作为过程证据。不得自动移动、改名、删除或重新分类项目文件；目录整理和历史文件清理由用户自行处理，只有用户另行明确要求时才能执行。

## 自动生产线入口

- 用户在本目录的新对话中输入“开始”，或要求启动/继续完整短视频自动流程时，必须先完整读取并执行`工作流文档/自动生产线-v2/skills/short-video-autopilot/SKILL.md`，再读取该Skill按阶段指定的reference；禁止凭旧聊天摘要自行编排。
- V2.3固定五个人工关卡：文案确认、配音确认、剪辑脚本确认、成片预览后明确授权上传、用户手动发布；另在制作封面前确认`手帐版 / 用户自行制作`。任何确认不得省略。
- 输入“开始”后先完成环境预检；预检未通过不得开始素材分析。
- 正式短视频时长优先15–20秒；证据链确有需要时可以稍微超过，但必须说明原因。
- 默认配音链路为VoxCPM2整段生成自然表演源，再用CosyVoice 3 `inference_vc`转换为本人音色；完成ASR、完整试听和技术检查后交用户确认。改用MiniMax、Voicebox、CosyVoice直接生成或其他方案前必须先告知用户并得到确认。
- 配音确认后只生成剪辑脚本并交给用户；`confirmations.edit_script`未批准时，禁止启动帧场、创建时间线、导入素材或开始剪辑。
- 剪辑完成后必须在帧场打开本地草稿让用户完整检查；仅当用户明确说“下一步上传”“开始上传”或同等明确上传指令，并登记`confirmations.upload=approved`后，才能打开平台上传页或传输文件。“OK”“可以”“下一步”不得解释为上传授权。
- 剪辑过程中可以使用帧场、FFmpeg、Python或其他合适的本地工具，但禁止ChatCut。最终待确认版本必须进入帧场，并与实际待交付文件一致，供用户完整播放检查。
- 制作抖音/小红书封面前必须让用户选择：使用V2 Skill内的B版手帐人物3:4模板，或由用户自行制作。未确认前不得生成封面；用户自行制作时只检查文件、比例、清晰度、版本对应和平台兼容性。
- 最终发布按钮永远由用户点击。

## ConnectOnion浏览器更新门槛

- 每次准备启动ConnectOnion内置浏览器时，必须使用`工具环境/connectonion/bin/co-browser-daily`或`工具环境/connectonion/bin/co-browser-publish`；禁止直接调用`co browser`绕过更新。
- 启动器必须先执行ConnectOnion在线升级，记录升级前后版本，并检查浏览器命令可用；全部成功后才能启动浏览器。
- 升级、联网或检查失败时必须停止，不得继续使用旧版本浏览器。更新日志只记录时间、版本和结果，不得记录账号、Cookie或密钥。

## 提示词备份

- V2提示词原件保存在`工作流文档/自动生产线-v2/`，不得再维护第二份V2副本。
- 全局`viral-machine-video-analyzer`和`machine-video-preflight` Skill的可查看备份分别保存在`工作流文档/爆款分析提示词/`和`工作流文档/机器视频前期与验收/`。
- 修改这两套全局Skill后，必须同步备份中的`SKILL.md`、`references/`、`scripts/`（如有）和`agents/openai.yaml`并逐文件验证一致；备份不能反向覆盖全局执行源。
